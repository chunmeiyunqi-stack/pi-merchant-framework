const fs = require('fs');

// 1. 读取您的源代码文件 (请根据实际文件名调整，如果是 .txt 请改为 .txt)
const sourceFile = 'source_code_v2.0.0_copyright_final.md'; 
const content = fs.readFileSync(sourceFile, 'utf8');

// 2. HTML 模板 (包含软著标准 CSS)
const htmlTemplate = `<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>先锋人工智能服务框架软件 V2.0.0 - 源代码</title>
    <style>
        /* 页面设置：A4 纸，自定义页眉页脚 */
        @page {
            size: A4;
            margin: 2cm 2cm 2.5cm 2cm; /* 上 左右 下 */
            @top-center {
                content: "先锋人工智能服务框架软件 V2.0.0";
                font-family: "SimSun", "Songti SC", serif;
                font-size: 10.5pt;
                color: #000;
            }
            @bottom-center {
                content: "- " counter(page) " -";
                font-family: "SimSun", "Songti SC", serif;
                font-size: 10.5pt;
                color: #000;
            }
        }
        body {
            /* 字体：英文代码 Consolas，中文注释自动回退宋体 */
            font-family: "Consolas", "Monaco", "Courier New", monospace;
            font-size: 10pt; /* 约10.5磅（小四） */
            line-height: 1.15; /* 关键：控制行高，1.15 约等于每页 50-54 行 */
            color: #000;
            white-space: pre-wrap; /* 保留换行和空格，长代码自动换行 */
            word-wrap: break-word;
            margin: 0;
            padding: 0;
        }
        pre {
            margin: 0;
            font-family: inherit;
        }
    </style>
</head>
<body>
<pre>${content.replace(/</g, '&lt;').replace(/>/g, '&gt;')}</pre>
</body>
</html>`;

// 3. 输出文件
fs.writeFileSync('copyright_print.html', htmlTemplate);
console.log('✅ HTML 文件已生成：copyright_print.html');
console.log('👉 下一步：在 Chrome 浏览器中打开此文件，按 Ctrl+P 打印为 PDF。');