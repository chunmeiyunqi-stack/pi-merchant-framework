global:
  scrape_interval: 10s

scrape_configs:
  - job_name: 'pi_merchant_framework'
    metrics_path: /api/metrics
    scheme: http
    static_configs:
      - targets: ['host.docker.internal:3000']
